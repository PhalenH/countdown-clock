import React, { useState } from "react";
import FutureEvents from "../components/FutureEvents";

const Profile = () => {
    return (
      <div>
          <FutureEvents futureEvents={profile.futureEvents} />
      </div>
    );
  };
  
  export default Profile;